const config = {
    version: "1.0.0",

    online: true,
    allow_video: true,
    allow_share: true,
    allow_navigator: true, 
};

const gameData = {
    updateTimestamp: 0,
    soundEnabled: true,
    musicEnabled: true,

    level: 1,
};

const user = {

};

const Global = {
    config,
    gameData,
    user,
};

export default Global;